# 工程信息快照：NZMobile（UE4.24.2）

**日期**：2026-02-28
**用途**：UE4 兼容性验证的参考工程，也是 install.py Phase A 的第一个测试目标

---

## 工程基础信息

| 项目 | 值 |
|------|---|
| 工程根目录 | `E:\kazeli_KAZELI-PC0_S1_2\NZMobile` |
| .uproject | `NZM.uproject` |
| EngineAssociation | `""` （源码编译） |
| 引擎根目录 | `E:\kazeli_KAZELI-PC0_S1_2` |
| 引擎版本 | UE **4.24.2**（Branch: `++UE4+Release-4.24`） |
| 引擎类型 | 源码编译（含 `UE4.sln`） |

## 引擎路径结构

```
E:\kazeli_KAZELI-PC0_S1_2\
├── Engine/
│   └── Build/Build.version   ← 版本文件
├── NZMobile/
│   └── NZM.uproject          ← 目标工程
│   └── Plugins/              ← 安装目标目录
├── GenerateProjectFiles.bat  ← 重新生成 VS 工程用
└── UE4.sln
```

## 模块统计（25+ 模块）

类型分布：Runtime / ClientOnly / ServerOnly / UncookedOnly / Editor / Program

## 插件统计（100+ 插件）

与 UnrealMCP 潜在冲突：无（名称不重复）

## UE4 兼容性关键限制

| 功能 | 状态 | 处理方式 |
|------|------|---------|
| Enhanced Input | ❌ UE4 无此模块 | ProjectCommands 用 `#if MCP_ENHANCED_INPUT_SUPPORTED` 排除 |
| BlueprintEditorLibrary | ❌ UE4 无此模块 | Build.cs 条件排除 |
| FAppStyle | ❌ UE4 使用 FEditorStyle | `UnrealMCPCompat.h` 宏 `MCP_STYLE_NAME` |
| IWYUSupport | ❌ UE4 使用 bEnforceIWYU | Build.cs 版本分支 |
| LiveCoding | ✅ UE4.22+ 可用 | 无需处理 |
| UEditorSubsystem | ✅ UE4.22+ 可用 | 无需处理 |
| UToolMenus | ✅ UE4.22+ 可用 | 无需处理 |

## install.py 调用示例（待 Phase A 完成后使用）

```bash
python Python/scripts/install.py \
  "E:/kazeli_KAZELI-PC0_S1_2/NZMobile/NZM.uproject" \
  --mcp-client claude \
  --engine-root "E:/kazeli_KAZELI-PC0_S1_2"
```
